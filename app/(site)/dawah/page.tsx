import { ClusterIndex } from "@/components/cluster-index";

export const metadata = {
  title: "Da'wah — Inviting to the Path with Wisdom",
  description: "A guide to da'wah: principles, methods, and resources for sharing the message of Islam.",
};

export default function Page() {
  return <ClusterIndex cluster="dawah" />;
}
